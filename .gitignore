# Local / tooling (not for publication)
.orchestrator/

# Cursor agent skills (local; not published)
.cursor/skills/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
# Root-only (avoid hiding docs/examples/*/dist from Vite/Svelte/Vue builds)
/dist/
/build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.venv/
venv/

# Aider (AI coding assistant artifacts)
.aider*

# Mis-placed alignment output (safety net if an agent writes at git root)
alignment_result.json
.alignment_result.json

# Node / npm (nested examples or JS tooling)
node_modules/
